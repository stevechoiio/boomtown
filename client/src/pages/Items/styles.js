const styles = theme => ({
  root: {
    flexGrow: 1
  },
  paper: {
    height: 300,
    width: 400,
    margin: 20
  },
  control: {
    padding: theme.spacing.unit * 2
  }
});
export default styles;
